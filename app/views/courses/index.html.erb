<h2>All Courses</h2>
<ul>
<% @courses.each do |course| %>
  <li>
    <%= course.name %>
    <%= link_to "View Course", course_path(course), class: "btn btn_link" %>
  </li>
<% end %>
</ul>
<%= link_to "Add Course", new_course_path, class: "btn btn-info" %>
